{
  "name": "firebrox",
  "version": "1.0.0",
  "description": "FIREBROX — a Clash Royale-style real-time arena card battler. No build step, just the web platform.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node test/sim.mjs && node test/dom-smoke.mjs && node scripts/build.mjs --check"
  },
  "license": "MIT"
}
